================
SET simple variable
================

set MY_VAR=hello

---

(program
  (variable_assignment
    (set_keyword)
    (assignment_variable)
    (assignment_string)))

================
SET quoted variable
================

set "MY_VAR=hello world"

---

(program
  (variable_assignment
    (set_keyword)
    (assignment_variable)
    (quoted_assignment_string)))

================
SET /a arithmetic
================

set /a result=5

---

(program
  (variable_assignment
    (set_keyword)
    (arithmetic_assignment
      (arithmetic_expression))))

================
SET /a complex expression
================

set /a x=y+1

---

(program
  (variable_assignment
    (set_keyword)
    (arithmetic_assignment
      (arithmetic_expression))))

================
SET /a quoted expression
================

set /a "a=b*c"

---

(program
  (variable_assignment
    (set_keyword)
    (arithmetic_assignment
      (arithmetic_expression))))

================
SET /p prompt
================

set /p answer=Enter value:

---

(program
  (variable_assignment
    (set_keyword)
    (prompt_assignment
      (assignment_variable)
      (assignment_string))))

================
SET /p empty prompt
================

set /p name=

---

(program
  (variable_assignment
    (set_keyword)
    (prompt_assignment
      (assignment_variable))))

================
SET variable with multiple quoted backslash paths
================

SET python_loc="\bin\python" "\32\bin\python" "\Python\python"

---

(program
  (variable_assignment
    (set_keyword)
    (assignment_variable)
    (assignment_string)))

================
SET variable reference in unquoted value
================

SET path=%userprofile%\scripts

---

(program
  (variable_assignment
    (set_keyword)
    (assignment_variable)
    (assignment_string
      (variable_reference))))

================
SET variable reference in quoted SET form
================

SET "path=%userprofile%\scripts"

---

(program
  (variable_assignment
    (set_keyword)
    (assignment_variable)
    (quoted_assignment_string
      (variable_reference))))

================
SET variable with parens in variable name
================

SET path=%programfiles(x86)%\app

---

(program
  (variable_assignment
    (set_keyword)
    (assignment_variable)
    (assignment_string
      (variable_reference))))

================
SET multiple variable references in assignment
================

SET cmd=%windir%\system32\%tool%.exe

---

(program
  (variable_assignment
    (set_keyword)
    (assignment_variable)
    (assignment_string
      (variable_reference)
      (variable_reference))))

================
SET /a quoted arithmetic percent is not a variable reference
================

set /a "x %=5"

---

(program
  (variable_assignment
    (set_keyword)
    (arithmetic_assignment
      (arithmetic_expression))))

================
SET variable with quoted paths containing variable references
================

SET gimp_loc="%userprofile%\AppData\Local\Programs\GIMP" "%programfiles(x86)%\GIMP" "%programfiles%\GIMP"

---

(program
  (variable_assignment
    (set_keyword)
    (assignment_variable)
    (assignment_string
      (variable_reference)
      (variable_reference)
      (variable_reference))))
